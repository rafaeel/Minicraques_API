<h1>Listing favoritos</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Nationality</th>
    <th>Photo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @favoritos.each do |favorito| %>
  <tr>
    <td><%= favorito.name %></td>
    <td><%= favorito.nationality %></td>
    <td><%= image_tag favorito.photo_url %></td>
    <td><%= link_to 'Show', favorito %></td>
    <td><%= link_to 'Edit', edit_favorito_path(favorito) %></td>
    <td><%= link_to 'Destroy', favorito, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Favorito', new_favorito_path %>
